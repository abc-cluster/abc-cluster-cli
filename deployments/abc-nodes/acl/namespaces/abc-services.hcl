name        = "abc-services"
description = "Core abc infrastructure namespace (storage, observability, ingress, auth, secrets)"

meta {
  group        = "platform"
  priority     = "infrastructure"
  job_priority = "90"
}
